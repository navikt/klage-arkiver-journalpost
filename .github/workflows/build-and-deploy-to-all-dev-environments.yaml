name: Build and deploy to all dev environments

on:
  push:

jobs:
  build-feature:
    uses: navikt/klage-arkiver-journalpost/.github/workflows/build.yaml@master
    secrets: inherit
  deploy-feature:
    uses: navikt/klage-arkiver-journalpost/.github/workflows/deploy-to-all-dev-environments.yaml@master
    needs: build-feature
    secrets: inherit
    with:
      image: ${{ needs.build-feature.outputs.image }}
